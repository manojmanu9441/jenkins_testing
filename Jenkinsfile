@Library('jenkins_testing') _

pipeline {
    agent any
    stages {
        stage('Greet') {
            steps {
                script {
                    sayHello('Jenkins')
                }
            }
        }
    }
}
